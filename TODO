Needed:
- visualiser gui + part for the things
- refactor rota + rota bug -> rewrite ?
- image utilities in the nn module & allow them to take rects as inputs
- classifier utilities like accuracy & classifier loss
- save title -> name-[archi]_cost_accuracy
- train the mnist & adapt the gui
- pooling layer


Later:
- Convolution layer
- multithreading / GPU
- option to save the costs for momentum ?
- dataset with any type, and f64 cast on forward/backprop

